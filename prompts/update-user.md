## 现有 Wiki 目录结构
```json
{{outline}}
```

## 自上次 Wiki 生成的代码变更
以下文件发生了变化（文件列表 + 变更行号，已通过文件依赖匹配筛选）：

{{changedSummary}}

## 受影响候选页面（仅供参考，不完全准确）
以下页面**可能**受上述变更影响（基于文件行号依赖匹配）。请自行验证确认。
如果某个页面确定受影响但不在此列表中，也可以加入 update。

{{candidatesInfo}}

## 旧版本页面全文（仅候选页面，用于判断变更是否影响页面内容）
{{pageContents}}

## 新增文件
以下为新增的源代码文件（不在现有 Wiki 的任何页面依赖中），请判断是否需要新增 Wiki 页面：
{{newFiles}}

请分析上述变更对现有 Wiki 的影响，输出 JSON。
